Raw WSI
   ↓
Uniformly sample many 256×256 patches
   ↓
Patch-level Foundation Model
   ↓
Patch embeddings X ∈ R^(N×C)
   ↓
K-means clustering
   ↓
从每个 cluster 中选 representative patches
   ↓
Candidate Summary Set X_k
   ↓
随机 Mask 一部分 candidate patches
   ↓
保留 visible patches
   ↓
Encoder
   ↓
visible latent features Z + CLS token
   ↓
┌───────────────────────────────┐
│                               │
│ Low-level branch              │ High-level branch
│                               │
│ Decoder                       │ CLS_MAE
│ ↓                             │ ↓
│ reconstruct masked embeddings │ 对齐多个 WSI Foundation Models
│ ↓                             │ 的全局语义 target
│ L_re                          │ ↓
│                               │ L_dis
└──────────────┬────────────────┘
               ↓
        L = L_re + L_dis
               ↓
           训练完成

================ Inference ================

同一张 WSI
   ↓
得到 cluster-based candidate set
   ↓
随机生成 n 组不同 mask
   ↓
每一组 visible patches → Encoder
   ↓
得到 CLS_MAE^(i)
   ↓
和全 WSI 的 global semantic target ũ 算 cosine similarity
   ↓
选择 similarity 最大的那一组
   ↓
保留下来的 unmasked patches
   ↓
Final WSI Summary
   ↓
原始 patch image + **spatial** coordinates

